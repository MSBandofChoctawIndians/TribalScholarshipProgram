import React from "react";

const WeaveBand: React.FC = () => {
  return (
    <div
      style={{
        backgroundImage: "url(image/weave-band.svg)",
        height: "40px",
        width: "100%",
      }}
    ></div>
  );
};

export default WeaveBand;
